import { MateProfileScreen } from '../../features/mate/MateProfileScreen';

// Mate · Profil tab — thin route wrapper; all content lives in the feature.
export default function MateProfile() {
  return <MateProfileScreen />;
}
