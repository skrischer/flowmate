import { MateAttunementScreen } from '../../features/mate/MateAttunementScreen';

// Mate · Eingestimmt tab — thin route wrapper; all content lives in the feature.
export default function MateIndex() {
  return <MateAttunementScreen />;
}
