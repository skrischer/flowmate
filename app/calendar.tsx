import { CalendarScreen } from '../features/flower/CalendarScreen';

// Owner/Flower month calendar route: a thin entry point so the screen lives in
// the feature (matches index / periods / mood-log).
export default function Calendar() {
  return <CalendarScreen />;
}
